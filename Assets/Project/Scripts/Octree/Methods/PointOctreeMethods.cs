using System.Collections.Generic ;
using Unity.Entities ;
using Unity.Collections ;
using Unity.Mathematics ;
using UnityEngine ;

namespace ECS.Octree.Point
{
    class PointOctreeMethods
    {

        /*
	    /// <summary>
	    /// Constructor for the point octree.
	    /// </summary>
	    /// <param name="initialWorldSize">Size of the sides of the initial node. The octree will never shrink smaller than this.</param>
	    /// <param name="initialWorldPos">Position of the centre of the initial node.</param>
	    /// <param name="f_minNodeSize">Nodes will stop splitting if the new nodes would be smaller than this.</param>
	    static public void _PointOctree ( float initialWorldSize, float3 f3_initialWorldPos, float f_minNodeSize) 
        {
		    if (f_minNodeSize > initialWorldSize) {
			    Debug.LogWarning("Minimum node size must be at least as big as the initial world size. Was: " + f_minNodeSize + " Adjusted to: " + initialWorldSize);
			    f_minNodeSize = initialWorldSize;
		    }
		    Count = 0;
		    initialSize = initialWorldSize;
		    minSize = f_minNodeSize;
		    rootNode = new PointOctreeNode<T>(initialSize, minSize, initialWorldPos);
	    }
        */

	    

    }

}